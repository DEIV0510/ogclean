/* ============================================================
   OGCLEAN · TÉRMINOS Y CONDICIONES
   Página de contenido estático: solo header/menú/carrito/footer
   compartidos, sin lógica de tienda.
   ============================================================ */

import { initComun, alCargar } from './comun.js';

alCargar(() => {
  initComun();
});
